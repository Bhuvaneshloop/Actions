name: Conditions
on: 
  push:
    branches:
      - main
  pull_request:
    types:
      - opened
      - synchronize
jobs:  
  build:
    runs-on: ubuntu-latest
    if: github.event_name == 'push'
    steps:
      - name: building
        run: echo "building the proejct"
  deploy:
    runs-on: ubuntu-latest
    if: github.event_name ==  'pull_request'
    steps:
      - name: depolyment
        run: echo "deploying the project"
