name: ENV_TRAIL
on: 
  workflow_dispatch:
    inputs:
      environment:
        description: app running environemnt
        required: true
        default: dev
        type: choice
        options:
          - dev
          - prod
          - uat
jobs:
  envvar:
    runs-on: ubuntu-latest
    steps:
      - name: Running Environment
        run: echo "Running in the environemnt ${{ inputs.environment }}"
